bytecontainer in lenprefix.txt and callback system, now fixed
- ...but check if the same issue affects non-bytes data types as well
- ...yes, it affects
- ...but now it should be fixed
Shortcut for SCCLL(1)
